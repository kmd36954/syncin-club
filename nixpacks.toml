[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@8", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter share-the-ride build"]

[start]
cmd = "node artifacts/api-server/dist/index.js"
