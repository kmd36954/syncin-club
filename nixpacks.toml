[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = [
  "corepack enable",
  "pnpm install"
]

[phases.build]
cmds = [
  "pnpm --filter share-the-ride build"
]

[start]
cmd = "pnpm --filter share-the-ride preview --host"
